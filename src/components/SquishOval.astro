---
import Oval from "../layouts/Oval.astro";
const { squareness, id, classes=[], link, color, image, contain, altText } = Astro.props;
classes.push("squish")
---

<Oval squareness={squareness} id={id} classes={classes} link={link} color={color} image={image} altText={altText}></Oval>